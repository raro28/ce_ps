@using Web.Controllers
<textarea disabled>@ShowNumberList(Model.Container.Data)</textarea>
@helper ShowNumberList(IEnumerable<double> list)
{
    var text = "";
    foreach (var item in list) {
        text += item + " ,";
    } 
    text=text.Substring(0,text.Length-2);
@text
}